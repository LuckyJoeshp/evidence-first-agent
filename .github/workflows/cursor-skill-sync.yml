name: Cursor skill in sync

# Fails if .cursor/skills/evidence-first/SKILL.md drifts from the canonical
# skills/evidence-first/SKILL.md. The .cursor copy is a real file, not a
# symlink, so Windows clones and GitHub ZIP downloads work (#55).
on:
  pull_request:
    paths:
      - skills/evidence-first/SKILL.md
      - .cursor/skills/**
      - .github/workflows/cursor-skill-sync.yml
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if the .cursor copy differs from SKILL.md
        run: |
          cmp skills/evidence-first/SKILL.md .cursor/skills/evidence-first/SKILL.md || {
            echo "::error::.cursor copy is out of sync. Run: cp skills/evidence-first/SKILL.md .cursor/skills/evidence-first/SKILL.md"
            exit 1
          }
